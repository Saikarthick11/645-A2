apiVersion: v1
kind: Service
metadata:
  name: swe-645-a2-service
spec:
  type: NodePort
  selector:
    # This MUST match the 'app' label in deployment.yaml exactly
    app: studentsurvey
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080